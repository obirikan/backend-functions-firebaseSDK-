    // const url = 'https://api.zippopotam.us/us/';
    // // Define the zip code you want to look up
    // const zipCode = zipcode;
    // // Make the API request using Axios
    // await axios.get(`${url}${zipCode}`).then((response) => {
    //     // Access the city name from the API response
    //     const cityName =response.data.places[0]["place name"];
    //     console.log(`The city name for zip code ${zipCode} is ${cityName}.`);
    //     register(email, password, username, zipcode, cityName, address);
    //   }).catch((error) => {
    //     console.log(error);
    //   });